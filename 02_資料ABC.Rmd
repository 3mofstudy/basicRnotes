# 基礎R- 資料ABC

## 資料賦值

資料的賦值，又稱為變數的設定。在進入R領域前，需要理解何謂是變數，變數就是可變動的數值，設定方式為變數名稱。在R中`<-` 及`=`符號皆可被用來賦予變數，但R科學家更常使用`<-`，在RStudio中，可用快速鍵`Alt`+`-` 快速形成

```{r echo=TRUE}
a <- 10
b = 15
a+b
```

## 常見資料型態

### 字串(character)

字串可用單引號或雙引號包起來代表，何時使用單引號或雙引號其實是隨意，只是需要注意一點，當單引號亦視為字串中內容時，外圍則須用雙引號包起來，反之亦然

```{r echo=TRUE}
name <- "Mary Wang"
name
citizenship <- 'Japan'
citizenship
sentences <- "I'm an adult"  
sentences
```

### 數值(numeric)

### 日期/時間 (Date/Time)

### 布林值(logic)

## 特殊資料型態

### 因素(factor)

## 資料運算子
